import 'package:flutter/material.dart';

class AlbumsPage extends StatelessWidget {
  build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Center(
          child: Text("AlbumsPage"),
        ),
      ),
    );
  }
}
